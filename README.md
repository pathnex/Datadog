Hey All, This Repo is used to create monitors on Datadog.

1. Create the Ec2 on AWS using "Terraform Ec2" directory
   
2. Install Ansible on those hosts using "Ansible" directory

3. Create monitors using "Terraform DD Monitors" directory

   NOTE: You have to change the values, so change them according to your env.

